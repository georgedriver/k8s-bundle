cluster:
  provider: docker-for-mac
  name: k8s-mirana
  codename: k8s-mirana
  domain: mirana.cndriver.tech
  environment: dev
  location: cn-shanghai
  ingress_class: nginx
  cluster_issuer: letsencrypt-prod

ingress:
  enabled: true

cert_manager:
  enabled: true

alidns_webhook:
  enabled: true
  # access/secret key from alicloud to control the DNS services https://dns.console.aliyun.com
  alicloud_access_key: {{ env "ALICLOUD_ACCESS_KEY" | default "not_set" }}
  alicloud_secret_key: {{ env "ALICLOUD_SECRET_KEY" | default "not_set" }}
  domain: cndriver.tech
  email: george9012@hotmail.com

vault:
  enabled: true

argocd:
  # Reset admin password: https://github.com/argoproj/argo-cd/issues/998#issuecomment-452908058
  enabled: true
  github_user: {{ env "ARGO_GITHUB_USER" | default "not_set" }}
  github_token: {{ env "ARGO_GITHUB_TOKEN" | default "not_set" }}
